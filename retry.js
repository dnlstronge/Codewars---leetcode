const wordsort = (str, wordDict) => {
  const maxlength = str.length

for(i=0; i< wordDict.length; i++) {
  
  let counter = 0
  let counted = 0
  let regex = new RegEx(wordict[i], g)
  console.log
  }
}


wordsort("dogsanddogs", ["dogs", "xxxx"])
